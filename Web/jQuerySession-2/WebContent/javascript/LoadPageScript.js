/**
 * call to init function 
 */
$(document).ready(init);

/**
 * function to change text of label having class name hello to 'hello world'
 */
function init(){
    $("label.hello").text("hello world");
}
